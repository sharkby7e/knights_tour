<div class="w-full max-w-7xl min-[104rem]:max-w-[104rem] flex flex-col items-center">
  <ul class="w-full grid grid-cols-1 min-[70rem]:grid-cols-[repeat(2,32rem)] min-[70rem]:justify-center gap-10 min-[104rem]:grid-cols-[repeat(3,32rem)]">
    <%= render @tours %>
  </ul>

  <div class="mt-6 sm:mt-12 w-full pagy-nav">
    <%== pagy_nav(@pagy) %>
  </div>
</div>
