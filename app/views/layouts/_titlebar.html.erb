<div class="w-full flex items-center justify-between">
  <%= link_to "A Knight’s Tour", root_path, class: "font-title text-xl lg:text-2xl font-semibold text-zinc-200" %>
  <nav class="flex gap-3 lg:gap-6 text-sm lg:text-lg">
    <%= link_to "Play", root_path, class: current_page?(root_path) ? "text-accent font-semibold" : "text-zinc-300" %>
    <%= link_to "Tours", tours_path, class: current_page?(tours_path) ? "text-accent font-semibold" : "text-zinc-300" %>
  </nav>
</div>
