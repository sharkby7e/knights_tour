<div class="w-64 flex flex-col gap-3 lg:gap-6">
  <div class="h-16 lg:h-24 lg:w-full flex flex-col items-center justify-center gap-1">
    <%= top_slot %>
  </div>

  <%= render layout: "tours/transport", locals: { target_prefix: target_prefix } do %>
    <%= middle_button %>
  <% end %>

  <%= render "tours/ticker_row", target_prefix: target_prefix, total: total %>

  <%= render "tours/path_toggle", target_prefix: target_prefix %>

  <%= yield %>
</div>
